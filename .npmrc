# Workspace packages reference each other by their declared version range, resolved locally.
link-workspace-packages=true
# Fail installs that would otherwise silently pick a non-workspace version.
prefer-workspace-packages=true
# Keep the dependency tree strict — no phantom (undeclared) dependencies.
auto-install-peers=false
strict-peer-dependencies=false
