/**
 * Generated by orval v8.17.0 🍺
 * Do not edit manually.
 * HDM Bridge API
 * Local HTTP bridge for the Armenian fiscal cash register (HDM) protocol. Every operation is a `POST /v1/<op>` carrying a uniform JSON envelope `{ connection?, params? }`: `connection` overrides the bridge's configured default device field-by-field, and `params` carries the operation input. On failure every route returns the same error envelope (see `ErrorBody`): `kind` is a stable machine tag and `code` carries the device/spec response code when the device rejected the request. All routes except `/v1/health`, `/v1/info`, and `/v1/openapi.json` require `Authorization: Bearer <token>`.
 * OpenAPI spec version: 0.3.0
 */
import * as zod from 'zod';

/**
 * Register a cash-drawer in or out. Requires connection: host + password + cashier + pin.
 * @summary Register a cash-drawer in or out.
 */
export const cashInOutBodyConnectionCashierMin = 0;

export const cashInOutBodyConnectionPortMin = 0;
export const cashInOutBodyConnectionPortMax = 65535;

export const cashInOutBodyConnectionTimeoutSecsMin = 0;

export const cashInOutBodyParamsCashierIdMin = 0;

export const cashInOutBodyParamsDescriptionDefault = ``;

export const CashInOutBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(cashInOutBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(cashInOutBodyConnectionPortMin).max(cashInOutBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(cashInOutBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.object({
  "amount": zod.number().describe('Amount; must be greater than zero. Sent as a JSON number on the wire.'),
  "cashierId": zod.number().min(cashInOutBodyParamsCashierIdMin).nullish().describe('Cashier number.'),
  "description": zod.string().default(cashInOutBodyParamsDescriptionDefault).describe('Free-text comment.'),
  "isCashIn": zod.boolean().describe('`true` for cash-in (deposit), `false` for cash-out (withdrawal).')
}).describe('Op 11 request: record a cash-drawer adjustment.')
})

export const CashInOutResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')


/**
 * Get the device date and time. Requires connection: host + password + cashier + pin.
 * @summary Get the device date and time.
 */
export const dateTimeBodyConnectionCashierMin = 0;

export const dateTimeBodyConnectionPortMin = 0;
export const dateTimeBodyConnectionPortMax = 65535;

export const dateTimeBodyConnectionTimeoutSecsMin = 0;



export const DateTimeBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(dateTimeBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(dateTimeBodyConnectionPortMin).max(dateTimeBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(dateTimeBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const DateTimeResponse = zod.object({
  "dt": zod.string().describe('Device date and time as a string. Spec does not pin the format; treat as opaque.')
}).describe('Op 12 response.')


/**
 * Validate a single eMark code. Requires connection: host + password + cashier + pin.
 * @summary Validate a single eMark code.
 */
export const emarkBodyConnectionCashierMin = 0;

export const emarkBodyConnectionPortMin = 0;
export const emarkBodyConnectionPortMax = 65535;

export const emarkBodyConnectionTimeoutSecsMin = 0;



export const EmarkBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(emarkBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(emarkBodyConnectionPortMin).max(emarkBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(emarkBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.object({
  "eMark": zod.string().describe('eMark code. Per spec §4.9: 29-110 chars, ASCII-printable only (33-126 plus 29 as\ndelimiter); `\"` escaped as `\\\"`, `\\` as `\\\\`, and ASCII 29 used as the group separator.')
}).describe('Op 16 request: submit a single eMark traceability code.')
})

export const EmarkResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')


/**
 * Configure receipt header and footer lines. Requires connection: host + password + cashier + pin.
 * @summary Configure receipt header and footer lines.
 */
export const headerFooterBodyConnectionCashierMin = 0;

export const headerFooterBodyConnectionPortMin = 0;
export const headerFooterBodyConnectionPortMax = 65535;

export const headerFooterBodyConnectionTimeoutSecsMin = 0;

export const headerFooterBodyParamsFootersItemAlignMin = 0;
export const headerFooterBodyParamsFootersItemAlignMax = 255;

export const headerFooterBodyParamsFootersItemFsizeMin = 0;
export const headerFooterBodyParamsFootersItemFsizeMax = 255;

export const headerFooterBodyParamsFootersDefault = [];
export const headerFooterBodyParamsHeadersItemAlignMin = 0;
export const headerFooterBodyParamsHeadersItemAlignMax = 255;

export const headerFooterBodyParamsHeadersItemFsizeMin = 0;
export const headerFooterBodyParamsHeadersItemFsizeMax = 255;

export const headerFooterBodyParamsHeadersDefault = [];

export const HeaderFooterBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(headerFooterBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(headerFooterBodyConnectionPortMin).max(headerFooterBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(headerFooterBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.object({
  "footers": zod.array(zod.object({
  "align": zod.number().min(headerFooterBodyParamsFootersItemAlignMin).max(headerFooterBodyParamsFootersItemAlignMax).describe('Alignment.'),
  "bold": zod.boolean().describe('Bold flag, serialised as a JSON boolean (`true`\/`false`) per the spec\'s field-type column\n(\"Boolean\"). The spec\'s §4.6.3 JSON example loosely writes it as the integer `1`, but a\nNewland N950 was verified to accept the boolean form (op 7 returned success).'),
  "fsize": zod.number().min(headerFooterBodyParamsFootersItemFsizeMin).max(headerFooterBodyParamsFootersItemFsizeMax).describe('Font size (1 through 5).'),
  "text": zod.string().describe('Text content.')
}).describe('A single line of header or footer text.')).default(headerFooterBodyParamsFootersDefault).describe('Footer lines, in print order (top to bottom).'),
  "headers": zod.array(zod.object({
  "align": zod.number().min(headerFooterBodyParamsHeadersItemAlignMin).max(headerFooterBodyParamsHeadersItemAlignMax).describe('Alignment.'),
  "bold": zod.boolean().describe('Bold flag, serialised as a JSON boolean (`true`\/`false`) per the spec\'s field-type column\n(\"Boolean\"). The spec\'s §4.6.3 JSON example loosely writes it as the integer `1`, but a\nNewland N950 was verified to accept the boolean form (op 7 returned success).'),
  "fsize": zod.number().min(headerFooterBodyParamsHeadersItemFsizeMin).max(headerFooterBodyParamsHeadersItemFsizeMax).describe('Font size (1 through 5).'),
  "text": zod.string().describe('Text content.')
}).describe('A single line of header or footer text.')).default(headerFooterBodyParamsHeadersDefault).describe('Header lines, in print order (top to bottom).')
}).describe('Op 7 request: configure receipt header and footer lines.')
})

export const HeaderFooterResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')


/**
 * @summary Liveness probe (public, no auth).
 */
export const HealthResponse = zod.object({
  "status": zod.string().describe('Always `\"ok\"`.')
}).describe('Liveness response for `GET \/v1\/health`.')


/**
 * @summary Bridge metadata and the operation list (public, no auth).
 */
export const InfoResponse = zod.object({
  "default_device_configured": zod.boolean().describe('Whether a default device connection is configured (so requests may omit `connection`).'),
  "name": zod.string().describe('Server name (`hdm-bridge`).'),
  "operations": zod.array(zod.string()).describe('The operation names the bridge exposes.'),
  "spec_version": zod.string().describe('HDM specification version the underlying client targets.'),
  "version": zod.string().describe('Bridge crate version.')
}).describe('Bridge metadata for `GET \/v1\/info`.')


/**
 * Verify operator login credentials. Requires connection: host + password + cashier + pin.
 * @summary Verify operator login credentials.
 */
export const loginBodyConnectionCashierMin = 0;

export const loginBodyConnectionPortMin = 0;
export const loginBodyConnectionPortMax = 65535;

export const loginBodyConnectionTimeoutSecsMin = 0;



export const LoginBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(loginBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(loginBodyConnectionPortMin).max(loginBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(loginBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const LoginResponse = zod.object({
  "ok": zod.boolean().describe('Whether the operation succeeded.')
}).describe('Boolean-outcome response (e.g. login confirmation).')


/**
 * Configure the receipt header logo. Requires connection: host + password + cashier + pin.
 * @summary Configure the receipt header logo.
 */
export const headerLogoBodyConnectionCashierMin = 0;

export const headerLogoBodyConnectionPortMin = 0;
export const headerLogoBodyConnectionPortMax = 65535;

export const headerLogoBodyConnectionTimeoutSecsMin = 0;



export const HeaderLogoBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(headerLogoBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(headerLogoBodyConnectionPortMin).max(headerLogoBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(headerLogoBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.object({
  "headerLogo": zod.string().describe('Logo image bytes encoded as a Base64 string. Per spec §4.6.4 the image must be in \*\*BMP\*\*\nformat with a colour depth of \*\*no more than 4 bits\*\* (≤16 colours; 1-bit monochrome is\nfine). Note: the English translation\'s \"must not exceed 4 pixels in height\" is a\nmistranslation of the original \"must not contain more than 4 bits of colour\".')
}).describe('Op 8 request: upload a header logo image (Base64-encoded BMP, colour depth ≤4 bits).')
})

export const HeaderLogoResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')


/**
 * @summary This OpenAPI 3.1 document (public, no auth).
 */
export const OpenapiDocumentResponse = zod.looseObject({

})


/**
 * List the HDM's operators and departments. Requires connection: host + password.
 * @summary List the HDM's operators and departments.
 */
export const operatorsBodyConnectionCashierMin = 0;

export const operatorsBodyConnectionPortMin = 0;
export const operatorsBodyConnectionPortMax = 65535;

export const operatorsBodyConnectionTimeoutSecsMin = 0;



export const OperatorsBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(operatorsBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(operatorsBodyConnectionPortMin).max(operatorsBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(operatorsBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const operatorsResponseCItemDepsItemMin = 0;

export const operatorsResponseCItemDepsDefault = [];
export const operatorsResponseCItemIdMin = 0;

export const operatorsResponseCItemNameDefault = ``;
export const operatorsResponseCDefault = [];
export const operatorsResponseDItemIdMin = 0;

export const operatorsResponseDItemNameDefault = ``;
export const operatorsResponseDItemTypeDefault = 0;
export const operatorsResponseDItemTypeMin = 0;

export const operatorsResponseDDefault = [];

export const OperatorsResponse = zod.object({
  "c": zod.array(zod.object({
  "deps": zod.array(zod.number().min(operatorsResponseCItemDepsItemMin)).default(operatorsResponseCItemDepsDefault).describe('Unique IDs of departments assigned to this operator.'),
  "id": zod.number().min(operatorsResponseCItemIdMin).describe('Operator\'s unique numeric ID, used as the cashier code in [`super::session::OperatorLoginRequest`].'),
  "name": zod.string().default(operatorsResponseCItemNameDefault).describe('Display name.')
}).describe('Single entry from the operator list.')).default(operatorsResponseCDefault).describe('Operators registered on the device (wire key `c`).'),
  "d": zod.array(zod.object({
  "id": zod.number().min(operatorsResponseDItemIdMin).describe('Department\'s unique numeric ID.'),
  "name": zod.string().default(operatorsResponseDItemNameDefault).describe('Display name.'),
  "type": zod.number().min(operatorsResponseDItemTypeMin).default(operatorsResponseDItemTypeDefault).describe('Taxation kind. Numeric on the wire; mapped to [`TaxationKind`].')
}).describe('Single entry from the department list.')).default(operatorsResponseDDefault).describe('Departments registered on the device (wire key `d`).')
}).describe('Op 1 response: operators and departments registered on the HDM device.\n\nThe wire keys are the cryptic `c` \/ `d` from the spec example; the Rust fields are named for\nwhat they hold.')


/**
 * List payment systems configured on the device. Requires connection: host + password + cashier + pin.
 * @summary List payment systems configured on the device.
 */
export const paymentSystemsBodyConnectionCashierMin = 0;

export const paymentSystemsBodyConnectionPortMin = 0;
export const paymentSystemsBodyConnectionPortMax = 65535;

export const paymentSystemsBodyConnectionTimeoutSecsMin = 0;



export const PaymentSystemsBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(paymentSystemsBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(paymentSystemsBodyConnectionPortMin).max(paymentSystemsBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(paymentSystemsBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const paymentSystemsResponsePaymentSystemsItemCodeMin = 0;

export const paymentSystemsResponsePaymentSystemsItemNameDefault = ``;
export const paymentSystemsResponsePaymentSystemsDefault = [];

export const PaymentSystemsResponse = zod.object({
  "PaymentSystems": zod.array(zod.object({
  "code": zod.number().min(paymentSystemsResponsePaymentSystemsItemCodeMin).describe('Payment-system code (used in [`super::receipt::PrintReceiptRequest::payment_system`]).'),
  "name": zod.string().default(paymentSystemsResponsePaymentSystemsItemNameDefault).describe('Display name.')
}).describe('A single payment-system entry from op 15. Codes are documented in spec §4.8 (1 = card,\n10-18 = various Armenian mobile wallets).')).default(paymentSystemsResponsePaymentSystemsDefault).describe('One entry per configured payment system.')
}).describe('Op 15 response: list of payment systems configured on the device.')


/**
 * Probe an endpoint and identify it as an HDM. Requires connection: host (port/timeout optional).
 * @summary Probe an endpoint and identify it as an HDM.
 */
export const probeBodyConnectionCashierMin = 0;

export const probeBodyConnectionPortMin = 0;
export const probeBodyConnectionPortMax = 65535;

export const probeBodyConnectionTimeoutSecsMin = 0;



export const ProbeBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(probeBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(probeBodyConnectionPortMin).max(probeBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(probeBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const probeResponseResponseCodeMin = 0;
export const probeResponseResponseCodeMax = 65535;

export const probeResponseProtocolVersion0ItemMin = 0;
export const probeResponseProtocolVersion0ItemMax = 255;
export const probeResponseProtocolVersion1ItemMin = 0;
export const probeResponseProtocolVersion1ItemMax = 255;
export const probeResponseSoftwareVersion0ItemMin = 0;
export const probeResponseSoftwareVersion0ItemMax = 255;
export const probeResponseSoftwareVersion1ItemMin = 0;
export const probeResponseSoftwareVersion1ItemMax = 255;
export const probeResponseSoftwareVersion2ItemMin = 0;
export const probeResponseSoftwareVersion2ItemMax = 255;



export const ProbeResponse = zod.object({
  "protocol_version": zod.tuple([zod.number().min(probeResponseProtocolVersion0ItemMin).max(probeResponseProtocolVersion0ItemMax),
zod.number().min(probeResponseProtocolVersion1ItemMin).max(probeResponseProtocolVersion1ItemMax)]).describe('HDM protocol version reported in the response header (major, minor).'),
  "response_code": zod.number().min(probeResponseResponseCodeMin).max(probeResponseResponseCodeMax).describe('Response code the device returned to the unauthenticated probe.\n\nCarried for diagnostics rather than as a success\/failure signal — reaching this struct\nalready means the endpoint is an HDM. A `403` ([`crate::ServerErrorKind::UnauthorizedConnection`])\nis the expected value during first contact and tells the operator the caller\'s IP must be\nregistered on the device\'s integration screen.'),
  "software_version": zod.tuple([zod.number().min(probeResponseSoftwareVersion0ItemMin).max(probeResponseSoftwareVersion0ItemMax),
zod.number().min(probeResponseSoftwareVersion1ItemMin).max(probeResponseSoftwareVersion1ItemMax),
zod.number().min(probeResponseSoftwareVersion2ItemMin).max(probeResponseSoftwareVersion2ItemMax)]).describe('HDM firmware\/software version (major, minor, patch).')
}).describe('Protocol identity reported by an endpoint that responded to [`identify`].')


/**
 * Print a fiscal receipt. Requires connection: host + password + cashier + pin.
 * @summary Print a fiscal receipt.
 */
export const printReceiptBodyConnectionCashierMin = 0;

export const printReceiptBodyConnectionPortMin = 0;
export const printReceiptBodyConnectionPortMax = 65535;

export const printReceiptBodyConnectionTimeoutSecsMin = 0;

export const printReceiptBodyParamsPaymentSystemMin = 0;

export const printReceiptBodyParamsDepMin = 0;

export const printReceiptBodyParamsEMarksDefault = [];
export const printReceiptBodyParamsItemsItemAdditionalDiscountTypeMin = 0;

export const printReceiptBodyParamsItemsItemDepMin = 0;

export const printReceiptBodyParamsItemsItemDiscountTypeMin = 0;

export const printReceiptBodyParamsItemsDefault = [];
export const printReceiptBodyParamsModeMin = 0;
export const printReceiptBodyParamsModeMax = 255;



export const PrintReceiptBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(printReceiptBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(printReceiptBodyConnectionPortMin).max(printReceiptBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(printReceiptBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.object({
  "PaymentSystem": zod.number().min(printReceiptBodyParamsPaymentSystemMin).nullish().describe('Payment system code from spec §4.8 (1 = card, 10-18 = various Armenian wallets). Used\nonly when `use_ext_pos = false` and the HDM has multiple payment systems configured.'),
  "dep": zod.number().min(printReceiptBodyParamsDepMin).nullish().describe('Department for simple\/prepayment receipts (when `items` is empty).'),
  "eMarks": zod.array(zod.string()).default(printReceiptBodyParamsEMarksDefault).describe('eMark traceability codes for marked goods (29-110 chars each, ASCII printable, escaping\nrules in spec §4.5.4). Used only in product-mode receipts.'),
  "items": zod.array(zod.object({
  "additionalDiscount": zod.number().nullish().describe('Stacked secondary discount amount (skip when no discount applies).'),
  "additionalDiscountType": zod.number().min(printReceiptBodyParamsItemsItemAdditionalDiscountTypeMin).nullish().describe('Stacked secondary discount kind (skip when no discount applies).'),
  "adgCode": zod.string().nullish().describe('ATG\/ADG tax classification code. Lookup at `taxservice.am`. Mandatory unless waived by\nitem-level discount logic per spec §4.5.4.'),
  "dep": zod.number().min(printReceiptBodyParamsItemsItemDepMin).describe('Department this item belongs to.'),
  "discount": zod.number().nullish().describe('Primary discount amount (skip when no discount applies).'),
  "discountType": zod.number().min(printReceiptBodyParamsItemsItemDiscountTypeMin).nullish().describe('Primary discount kind (skip when no discount applies).'),
  "price": zod.number().describe('Unit price, max 2 decimal places.'),
  "productCode": zod.string().describe('Vendor SKU (≤50 chars, must be non-empty).'),
  "productName": zod.string().describe('Display name (≤50 chars, must be non-empty).'),
  "qty": zod.number().describe('Quantity, max 3 decimal places.'),
  "unit": zod.string().describe('Unit of measure (≤50 chars, must be non-empty).')
}).describe('A single item in a printed receipt.')).default(printReceiptBodyParamsItemsDefault).describe('Items (required when `mode = Products`). Use an empty `Vec` for simple\/prepayment modes.'),
  "mode": zod.number().min(printReceiptBodyParamsModeMin).max(printReceiptBodyParamsModeMax).describe('Mode (simple \/ products \/ prepayment).'),
  "paidAmount": zod.number().describe('Cash portion of the payment.'),
  "paidAmountCard": zod.number().describe('Card (cashless) portion of the payment.'),
  "partialAmount": zod.number().describe('Partial-payment portion.'),
  "partnerTin": zod.string().nullish().describe('Partner TIN (8 digits) for B2B receipts. Sent as `null` if absent — the spec requires\nthis field to appear even when null.'),
  "prePaymentAmount": zod.number().describe('Prepayment-used portion.'),
  "rrn": zod.string().nullish().describe('Acquirer transaction RRN (12 chars). Used when `use_ext_pos = true`.'),
  "terminalId": zod.string().nullish().describe('Payment terminal unique ID (8 chars). Used when `use_ext_pos = true`.'),
  "useExtPOS": zod.boolean().describe('If `true`, supply `rrn` + `terminal_id` from an external POS terminal. If `false`, the\nHDM uses its own configured payment system (set `payment_system`).')
}).describe('Op 4 request: print a fiscal receipt. Encrypted with the session key.')
})

export const printReceiptResponseAddressDefault = ``;
export const printReceiptResponseChangeDefault = 0;
export const printReceiptResponseCrnDefault = ``;
export const printReceiptResponseEmarksCountDefault = null;
export const printReceiptResponseFiscalDefault = ``;
export const printReceiptResponseLotteryDefault = ``;
export const printReceiptResponsePrizeDefault = 0;
export const printReceiptResponsePrizeMin = 0;

export const printReceiptResponseQrDefault = null;
export const printReceiptResponseSnDefault = ``;
export const printReceiptResponseTaxpayerDefault = ``;
export const printReceiptResponseTimeDefault = 0;
export const printReceiptResponseTinDefault = ``;
export const printReceiptResponseTotalDefault = 0;
export const printReceiptResponseVerificationNumberDefault = null;

export const PrintReceiptResponse = zod.object({
  "address": zod.string().default(printReceiptResponseAddressDefault).describe('Taxpayer registered address.'),
  "change": zod.number().default(printReceiptResponseChangeDefault).describe('Change due to the customer.'),
  "crn": zod.string().default(printReceiptResponseCrnDefault).describe('HDM registration number (unique per device).'),
  "emarksCount": zod.string().nullish().default(printReceiptResponseEmarksCountDefault).describe('Number of eMark codes processed. Added post-v0.5. Documented as `string` in spec.'),
  "fiscal": zod.string().default(printReceiptResponseFiscalDefault).describe('Fiscal receipt number — the legally-binding identifier.'),
  "lottery": zod.string().default(printReceiptResponseLotteryDefault).describe('Lottery ticket number associated with the receipt.'),
  "prize": zod.number().min(printReceiptResponsePrizeMin).default(printReceiptResponsePrizeDefault).describe('`0` = no prize, `1` = prize won. (Currently no longer used by the lottery system.)'),
  "qr": zod.string().nullish().default(printReceiptResponseQrDefault).describe('QR-code payload to render on the printed receipt. Added post-v0.5.'),
  "rseq": zod.number().describe('HDM-assigned receipt sequence number.'),
  "sn": zod.string().default(printReceiptResponseSnDefault).describe('HDM hardware serial number.'),
  "taxpayer": zod.string().default(printReceiptResponseTaxpayerDefault).describe('Taxpayer legal name.'),
  "time": zod.number().default(printReceiptResponseTimeDefault).describe('Receipt timestamp (milliseconds since Unix epoch, Greenwich time).'),
  "tin": zod.string().default(printReceiptResponseTinDefault).describe('Taxpayer TIN.'),
  "total": zod.number().default(printReceiptResponseTotalDefault).describe('Total amount on the receipt.'),
  "verificationNumber": zod.string().nullish().default(printReceiptResponseVerificationNumberDefault).describe('Short alphanumeric verification number (≤13 chars) printed on the receipt. Added post-v0.5.')
}).describe('Op 4 response: fiscal data for a successfully-printed receipt.\n\nThree fields (`qr`, `verification_number`, `emarks_count`) were added in spec revisions past\nv0.5; they are marked `Option` with `serde(default)` so older HDM firmware still deserialises\nwithout errors.')


/**
 * Print a copy of the last receipt. Requires connection: host + password + cashier + pin.
 * @summary Print a copy of the last receipt.
 */
export const printLastReceiptBodyConnectionCashierMin = 0;

export const printLastReceiptBodyConnectionPortMin = 0;
export const printLastReceiptBodyConnectionPortMax = 65535;

export const printLastReceiptBodyConnectionTimeoutSecsMin = 0;



export const PrintLastReceiptBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(printLastReceiptBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(printLastReceiptBodyConnectionPortMin).max(printLastReceiptBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(printLastReceiptBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const PrintLastReceiptResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')


/**
 * Look up a returnable receipt's contents (read-only). Requires connection: host + password + cashier + pin.
 * @summary Look up a returnable receipt's contents (read-only).
 */
export const lookupReceiptBodyConnectionCashierMin = 0;

export const lookupReceiptBodyConnectionPortMin = 0;
export const lookupReceiptBodyConnectionPortMax = 65535;

export const lookupReceiptBodyConnectionTimeoutSecsMin = 0;



export const LookupReceiptBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(lookupReceiptBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(lookupReceiptBodyConnectionPortMin).max(lookupReceiptBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(lookupReceiptBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.object({
  "crn": zod.string().describe('HDM registration number of the device that printed the original receipt.'),
  "receiptId": zod.string().describe('Number of the receipt to look up.')
}).describe('Op 10 request: fetch the fiscal contents of a previously-issued receipt you intend to return.\n\nThis is a \*\*read-only lookup\*\*, not a refund. The original spec §4.5.6 is\n\"ՀԴՄ վերադարձվող կտրոնի ստացում\" = \*get the returnable receipt\*. The spec describes it in section\n§4.5.6, but its wire operation code is \*\*10\*\* per the operation-codes table (§4.4.1) — the section\nnumber is not the operation code. It returns the receipt\'s items, amounts, eMarks and sale type\nso a caller can construct the actual return via [`PrintReturnReceiptRequest`] (op 6). It registers\nnothing.')
})

export const lookupReceiptResponseCardDefault = null;
export const lookupReceiptResponseCashDefault = null;
export const lookupReceiptResponseCidDefault = null;
export const lookupReceiptResponseDidDefault = null;
export const lookupReceiptResponseEMarksDefault = [];
export const lookupReceiptResponsePTinDefault = null;
export const lookupReceiptResponsePpaDefault = null;
export const lookupReceiptResponsePpuDefault = null;
export const lookupReceiptResponseRefDefault = null;
export const lookupReceiptResponseRefcrnDefault = null;
export const lookupReceiptResponseRseqDefault = null;
export const lookupReceiptResponseSaleTypeDefault = null;
export const lookupReceiptResponseSubTypeDefault = null;
export const lookupReceiptResponseTaDefault = null;
export const lookupReceiptResponseTimeDefault = null;
export const lookupReceiptResponseTotalsItemAdscDefault = null;
export const lookupReceiptResponseTotalsItemDidDefault = null;
export const lookupReceiptResponseTotalsItemDscDefault = null;
export const lookupReceiptResponseTotalsItemDsctDefault = null;
export const lookupReceiptResponseTotalsItemDtDefault = null;
export const lookupReceiptResponseTotalsItemDtmDefault = null;
export const lookupReceiptResponseTotalsItemGcDefault = null;
export const lookupReceiptResponseTotalsItemGnDefault = null;
export const lookupReceiptResponseTotalsItemMuDefault = null;
export const lookupReceiptResponseTotalsItemPDefault = null;
export const lookupReceiptResponseTotalsItemQtyDefault = null;
export const lookupReceiptResponseTotalsItemRpidDefault = null;
export const lookupReceiptResponseTotalsItemTDefault = null;
export const lookupReceiptResponseTotalsItemTtDefault = null;
export const lookupReceiptResponseTotalsDefault = [];
export const lookupReceiptResponseTypeDefault = null;

export const LookupReceiptResponse = zod.object({
  "card": zod.number().nullish().default(lookupReceiptResponseCardDefault).describe('Card (cashless) paid.'),
  "cash": zod.number().nullish().default(lookupReceiptResponseCashDefault).describe('Cash paid.'),
  "cid": zod.number().nullish().default(lookupReceiptResponseCidDefault).describe('Cashier ID (`Գանձապահի ID`).'),
  "did": zod.number().nullish().default(lookupReceiptResponseDidDefault).describe('Department of a simple receipt.'),
  "eMarks": zod.array(zod.string()).default(lookupReceiptResponseEMarksDefault).describe('eMark codes of marked goods on the receipt.'),
  "pTin": zod.string().nullish().default(lookupReceiptResponsePTinDefault).describe('Buyer TIN (8 digits) for a B2B receipt, or `null`.'),
  "ppa": zod.number().nullish().default(lookupReceiptResponsePpaDefault).describe('Partial-payment amount.'),
  "ppu": zod.number().nullish().default(lookupReceiptResponsePpuDefault).describe('Used prepayment amount.'),
  "ref": zod.number().nullish().default(lookupReceiptResponseRefDefault).describe('When this receipt is itself a return, the number of the receipt it returned (`ref`).'),
  "refcrn": zod.string().nullish().default(lookupReceiptResponseRefcrnDefault).describe('crn of the HDM that printed the returned receipt (set only for return-type receipts).'),
  "rseq": zod.number().nullish().default(lookupReceiptResponseRseqDefault).describe('Receipt sequence number. The PDF field table calls this the return-receipt sequence number,\nbut Code Block 7 omits it.'),
  "saleType": zod.number().nullish().default(lookupReceiptResponseSaleTypeDefault).describe('Sale type: `0` sale, `2` return, `3` prepayment.'),
  "subType": zod.number().nullish().default(lookupReceiptResponseSubTypeDefault).describe('Receipt sub-type: `1` simple, `2` itemised. (In the field table only; absent from the example.)'),
  "ta": zod.number().nullish().default(lookupReceiptResponseTaDefault).describe('Total amount.'),
  "time": zod.number().nullish().default(lookupReceiptResponseTimeDefault).describe('Receipt registration\/print time (ms since epoch, Greenwich).'),
  "totals": zod.array(zod.object({
  "adsc": zod.number().nullish().default(lookupReceiptResponseTotalsItemAdscDefault).describe('Proportional secondary discount (`adsc`).'),
  "did": zod.number().nullish().default(lookupReceiptResponseTotalsItemDidDefault).describe('Department (`did`).'),
  "dsc": zod.number().nullish().default(lookupReceiptResponseTotalsItemDscDefault).describe('Primary discount (`dsc`).'),
  "dsct": zod.number().nullish().default(lookupReceiptResponseTotalsItemDsctDefault).describe('Discount type (`dsct`).'),
  "dt": zod.number().nullish().default(lookupReceiptResponseTotalsItemDtDefault).describe('Department VAT amount (`dt`).'),
  "dtm": zod.number().nullish().default(lookupReceiptResponseTotalsItemDtmDefault).describe('Department tax regime (`dtm`).'),
  "gc": zod.string().nullish().default(lookupReceiptResponseTotalsItemGcDefault).describe('Product code (`gc`).'),
  "gn": zod.string().nullish().default(lookupReceiptResponseTotalsItemGnDefault).describe('Product name (`gn`).'),
  "mu": zod.string().nullish().default(lookupReceiptResponseTotalsItemMuDefault).describe('Unit of measure (`mu`).'),
  "p": zod.number().nullish().default(lookupReceiptResponseTotalsItemPDefault).describe('Unit price (`p`).'),
  "qty": zod.number().nullish().default(lookupReceiptResponseTotalsItemQtyDefault).describe('Quantity (`qty`).'),
  "rpid": zod.number().nullish().default(lookupReceiptResponseTotalsItemRpidDefault).describe('Row sequence number (`rpid`) — the handle used for per-item partial returns in op 6.'),
  "t": zod.number().nullish().default(lookupReceiptResponseTotalsItemTDefault).describe('Line total excluding VAT (`t`).'),
  "tt": zod.number().nullish().default(lookupReceiptResponseTotalsItemTtDefault).describe('Line total including VAT (`tt`).')
}).describe('A single line item in a [`ReturnableReceiptResponse`] (`totals[]`). All fields optional\/lenient\nfor the same reason as the parent — modelled from spec §4.5.6 Code Block 7, unverified.')).default(lookupReceiptResponseTotalsDefault).describe('Line items (empty for simple\/prepayment receipts, where the spec sends `null`).'),
  "type": zod.number().nullish().default(lookupReceiptResponseTypeDefault).describe('Transaction type as the Code Block 7 example\'s `type` field (mirrors `sale_type`).')
}).describe('Op 10 response: the looked-up receipt\'s full fiscal contents.\n\n\*\*Unverified against hardware.\*\* An earlier integration test sent this lookup to op 6 and got\nvendor code 503 with an empty body, but op 6 is in fact the print-return code (this crate\npreviously had the two codes swapped), so that result does not characterise this lookup. The\nlookup also keys on a server-side `Receipt_ID` the firmware never exposes (op 4 omits the `qr`\nfield entirely), so it may still be hard to satisfy in practice. This\nstruct is therefore modelled purely from spec §4.5.6, which is internally inconsistent: its\nfield table and the Code Block 7 example disagree (the example adds `type`, omits\n`rseq`\/`subType`\/`refcrn`, and uses JSON numbers where the table says String). Fields follow the\nCode Block 7 example — whose numeric types match what real firmware sends for op 4 — and are all\noptional\/lenient. The raw decrypted payload is logged at TRACE so the true shape can be\nconfirmed once a device ever returns one.')


/**
 * Print an X or Z fiscal report. Requires connection: host + password + cashier + pin.
 * @summary Print an X or Z fiscal report.
 */
export const reportBodyConnectionCashierMin = 0;

export const reportBodyConnectionPortMin = 0;
export const reportBodyConnectionPortMax = 65535;

export const reportBodyConnectionTimeoutSecsMin = 0;

export const reportBodyParamsOneOneDeptIdMin = 0;

export const reportBodyParamsOneTwoCashierIdMin = 0;

export const reportBodyParamsOneThreeTransactionTypeIdMin = 0;

export const reportBodyParamsThreeReportTypeMin = 0;
export const reportBodyParamsThreeReportTypeMax = 255;



export const ReportBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(reportBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(reportBodyConnectionPortMin).max(reportBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(reportBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.union([zod.union([zod.object({
  "deptId": zod.number().min(reportBodyParamsOneOneDeptIdMin)
}).describe('Restrict to a single department (`deptId`).'),zod.object({
  "cashierId": zod.number().min(reportBodyParamsOneTwoCashierIdMin)
}).describe('Restrict to a single cashier (`cashierId`).'),zod.object({
  "transactionTypeId": zod.number().min(reportBodyParamsOneThreeTransactionTypeIdMin)
}).describe('Restrict to a transaction type — cash vs cashless (`transactionTypeId`).')]),zod.unknown()]).and(zod.object({
  "endDate": zod.number().describe('End of the report time range.'),
  "reportType": zod.number().min(reportBodyParamsThreeReportTypeMin).max(reportBodyParamsThreeReportTypeMax).describe('X or Z report.'),
  "startDate": zod.number().describe('Start of the report time range (epoch-style integer, per spec example).')
})).describe('Op 9 request: print a fiscal report.')
})

export const ReportResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')


/**
 * Print a return receipt. Requires connection: host + password + cashier + pin.
 * @summary Print a return receipt.
 */
export const printReturnBodyConnectionCashierMin = 0;

export const printReturnBodyConnectionPortMin = 0;
export const printReturnBodyConnectionPortMax = 65535;

export const printReturnBodyConnectionTimeoutSecsMin = 0;

export const printReturnBodyParamsReturnTicketIdMin = 0;



export const PrintReturnBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(printReturnBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(printReturnBodyConnectionPortMin).max(printReturnBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(printReturnBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.object({
  "cardAmountForReturn": zod.number().nullish().describe('Card amount to return (only set for a partial return).'),
  "cashAmountForReturn": zod.number().nullish().describe('Cash amount to return (only set for a partial return).'),
  "crn": zod.string().describe('HDM registration number of the device that printed the receipt.'),
  "eMarks": zod.array(zod.string()).optional().describe('eMark codes for marked goods.'),
  "prePaymentAmountForReturn": zod.number().nullish().describe('Prepayment amount to return (only set for a partial return).'),
  "returnItemList": zod.array(zod.object({
  "quantity": zod.number().describe('Quantity of this row\'s items to return.'),
  "rpid": zod.number().describe('Row sequence number of the item being returned.')
}).describe('Per-item entry in a partial-return request.')).optional().describe('Per-item return list (only set when partially returning an itemised receipt).'),
  "returnTicketId": zod.number().min(printReturnBodyParamsReturnTicketIdMin).describe('Number of the receipt to be returned.'),
  "rrn": zod.string().nullish().describe('Acquirer RRN (12 chars).'),
  "terminalId": zod.string().nullish().describe('Payment terminal ID (8 chars).')
}).describe('Op 6 request: print a return\/refund receipt — the operation that actually registers a return.\n\nWith no amounts\/items it returns the whole receipt; set the `\*_for_return` amounts and\/or\n`return_item_list` for a partial return. The original spec §4.5.7 is\n\"ՀԴՄ վերադարձի կտրոնի տպում\" = \*print return receipt\*. The spec describes it in section §4.5.7,\nbut its wire operation code is \*\*6\*\* per the operation-codes table (§4.4.1). The read-only lookup\nof the receipt being returned is op 10, [`GetReturnableReceiptRequest`].')
})

export const printReturnResponseAddressDefault = ``;
export const printReturnResponseChangeDefault = 0;
export const printReturnResponseCrnDefault = ``;
export const printReturnResponseEmarksCountDefault = null;
export const printReturnResponseFiscalDefault = ``;
export const printReturnResponseLotteryDefault = ``;
export const printReturnResponsePrizeDefault = 0;
export const printReturnResponsePrizeMin = 0;

export const printReturnResponseRtimeDefault = 0;
export const printReturnResponseSnDefault = ``;
export const printReturnResponseTaxpayerDefault = ``;
export const printReturnResponseTimeDefault = 0;
export const printReturnResponseTinDefault = ``;
export const printReturnResponseTotalDefault = 0;
export const printReturnResponseVerificationNumberDefault = null;

export const PrintReturnResponse = zod.object({
  "address": zod.string().default(printReturnResponseAddressDefault).describe('Taxpayer address.'),
  "change": zod.number().default(printReturnResponseChangeDefault).describe('Change.'),
  "crn": zod.string().default(printReturnResponseCrnDefault).describe('HDM registration number.'),
  "emarksCount": zod.string().nullish().default(printReturnResponseEmarksCountDefault).describe('Number of registered marks.'),
  "fiscal": zod.string().default(printReturnResponseFiscalDefault).describe('Fiscal number.'),
  "lottery": zod.string().default(printReturnResponseLotteryDefault).describe('Lottery number.'),
  "prize": zod.number().min(printReturnResponsePrizeMin).default(printReturnResponsePrizeDefault).describe('Prize indicator (currently always 0).'),
  "rseq": zod.number().describe('Receipt sequence number.'),
  "rtime": zod.number().default(printReturnResponseRtimeDefault).describe('Return-receipt timestamp (ms since epoch, Greenwich).'),
  "sn": zod.string().default(printReturnResponseSnDefault).describe('HDM hardware serial number.'),
  "taxpayer": zod.string().default(printReturnResponseTaxpayerDefault).describe('Taxpayer name.'),
  "time": zod.number().default(printReturnResponseTimeDefault).describe('Original receipt timestamp (ms since epoch, Greenwich).'),
  "tin": zod.string().default(printReturnResponseTinDefault).describe('Taxpayer TIN.'),
  "total": zod.number().default(printReturnResponseTotalDefault).describe('Total amount.'),
  "verificationNumber": zod.string().nullish().default(printReturnResponseVerificationNumberDefault).describe('Short verification number printed on the receipt.')
}).describe('Op 6 response: full receipt details + return-specific timestamps.')


/**
 * Print a sample receipt. Requires connection: host + password + cashier + pin.
 * @summary Print a sample receipt.
 */
export const receiptSampleBodyConnectionCashierMin = 0;

export const receiptSampleBodyConnectionPortMin = 0;
export const receiptSampleBodyConnectionPortMax = 65535;

export const receiptSampleBodyConnectionTimeoutSecsMin = 0;



export const ReceiptSampleBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(receiptSampleBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(receiptSampleBodyConnectionPortMin).max(receiptSampleBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(receiptSampleBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const ReceiptSampleResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')


/**
 * Synchronize the device clock. Requires connection: host + password + cashier + pin.
 * @summary Synchronize the device clock.
 */
export const timeSyncBodyConnectionCashierMin = 0;

export const timeSyncBodyConnectionPortMin = 0;
export const timeSyncBodyConnectionPortMax = 65535;

export const timeSyncBodyConnectionTimeoutSecsMin = 0;



export const TimeSyncBody = zod.object({
  "connection": zod.object({
  "cashier": zod.number().min(timeSyncBodyConnectionCashierMin).nullish().describe('Operator (cashier) numeric id for login.'),
  "host": zod.string().nullish().describe('HDM host (IP or name).'),
  "password": zod.string().nullish().describe('HDM access password (used to derive the password key).'),
  "pin": zod.string().nullish().describe('Operator PIN for login.'),
  "port": zod.number().min(timeSyncBodyConnectionPortMin).max(timeSyncBodyConnectionPortMax).nullish().describe('HDM TCP port; defaults to [`DEFAULT_PORT`].'),
  "timeout_secs": zod.number().min(timeSyncBodyConnectionTimeoutSecsMin).nullish().describe('Socket timeout in seconds; defaults to [`DEFAULT_TIMEOUT_SECS`], clamped to [`MAX_TIMEOUT_SECS`].')
}).optional().describe('Connection parameters with every field optional, used both for the configured default and for a\nper-request override. Secrets are redacted in `Debug`.'),
  "params": zod.unknown().optional().describe('Ignored for this operation.')
})

export const TimeSyncResponse = zod.looseObject({

}).describe('Marker response for operations that do not return a meaningful payload.\n\nThe wire-level response framing is still parsed (header + zero-byte or `{}` payload), but the\ncaller gets no fields to consume.')
